{
  "name": "aws-quiz-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted practice-exam portal built from local quiz HTML files, with SQLite-backed user accounts and exam history.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "cookie-parser": "^1.4.6",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
