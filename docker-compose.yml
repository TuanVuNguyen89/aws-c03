services:
  quiz-portal:
    build: .
    ports:
      - "80:3000"
    environment:
      - SESSION_SECRET=${SESSION_SECRET}
    volumes:
      - quiz-data:/app/data
    restart: unless-stopped

volumes:
  quiz-data:
